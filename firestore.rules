rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // WARNING: This is a temporary, insecure rule to allow development.
      // It allows any client to read and write to the database.
      // TODO: Implement proper authentication and secure these rules.
      allow read, write: if true;
    }
  }
}
