
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow reads and writes to the patients collection only for authenticated users
    match /patients/{patientId} {
      allow read, write: if request.auth != null;
    }
  }
}
