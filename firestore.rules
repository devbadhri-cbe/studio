
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to the patients collection for any authenticated user
    match /patients/{patientId} {
      allow read, write: if request.auth != null;
    }
  }
}
