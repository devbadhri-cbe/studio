
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to the entire patients collection and all
    // documents within it for any authenticated user.
    match /patients/{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
